Config = {}

Config.Starting = {
    Weather = "EXTRASUNNY",
    RainLevel = 0.0,
    Blackout = false,

    Hour = 7,
    Minute = 00,
    Second = 00,
}

Config.TimeCycleSpeed = 2                -- Indicates how fast the day/night time cycle should be in seconds - 2 is GTA-V's default which takes around 48 minutes IRL for a full day cycle

Config.CommandToOpenMenu = "weathermenu" -- Opens the weather menu if player is authorized
